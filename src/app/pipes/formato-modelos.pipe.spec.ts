import { FormatoModelosPipe } from './formato-modelos.pipe';

describe('FormatoModelosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoModelosPipe();
    expect(pipe).toBeTruthy();
  });
});
